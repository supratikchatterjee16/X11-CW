int pointerMotionHandler(XEvent event){
//	Window root;
//	int x,y, winx, winy;
//	unsigned int i;
//	if(XQueryPointer(display, window, &window, &root, &x, &y, &winx, &winy, &i)==1){
//		std::cout<<"Mouse moved to"<<winx<<", "<<winy<<std::endl;
//	}
	return 0;
}
